# Implementation Notes: Issue #83 PNG preview support

## Task T1: Add viewable image helpers

- **Status:** Complete
- **Files Changed:** `src/lib/file-utils.ts`, `src/lib/file-utils.test.ts`
- **Tests Passed:** `./harness test`
- **Tests Failed:** 0

### Changes Summary

- Added `isViewableImage(filename)` for PNG, JPG/JPEG, GIF, WebP, ICO, and SVG.
- Added `getImageMimeType(filename)` with supported image MIME mappings and fallback to `application/octet-stream`.
- Preserved `isBinaryFile` classification for image extensions.

## Task T2: Return base64 data URLs for viewable images from the content API

- **Status:** Complete
- **Files Changed:** `src/app/api/files/content/route.ts`, `src/app/api/files/content/route.test.ts`
- **Tests Passed:** `./harness test`
- **Tests Failed:** 0

### Changes Summary

- Added a viewable-image GET branch before the opaque binary fallback.
- Enforced the 1 MB image preview cap with HTTP 413 and `FILE_TOO_LARGE`.
- Returned base64 data URLs with `language: "image"` and `isBinary: true`.
- Covered PNG, JPG, SVG, oversized PNG, ZIP fallback, PNG PUT guard, and worktree-root image reads.

## Task T3: Render image content in FileViewer

- **Status:** Complete
- **Files Changed:** `src/components/file-viewer.tsx`, `src/components/file-viewer.test.tsx`
- **Tests Passed:** `./harness test`
- **Tests Failed:** 0

### Changes Summary

- Added an `ImageView` render branch before the generic binary fallback.
- Rendered image previews with centered, constrained `<img>` output and alt text containing the file name.
- Updated component tests so generic binary coverage uses `archive.zip`.
- Added PNG render, edit-control hiding, binary-placeholder hiding, and image worktree request coverage.

## Task T4: Consolidate regression tests for editing, binary guards, and worktree behavior

- **Status:** Complete
- **Files Changed:** affected test files above
- **Tests Passed:** `./harness lint`, `./harness test`
- **Tests Failed:** 0

### Changes Summary

- Preserved existing edit/save success and failure tests, including `refreshFileTree()` success-only behavior.
- Preserved binary PUT guard coverage for `image.png`.
- Preserved active worktree GET, PUT, and diff request behavior with added image-specific GET coverage.

## Task T5: Verify the completed implementation

- **Status:** Complete
- **Files Changed:** `.harness/evidence/*` generated by harness, implementation notes
- **Tests Passed:** `./harness verify`
- **Tests Failed:** 0

### Test Results

- `./harness verify` passed lint, format check, build, test, and smoke.
- An initial verify run was degraded because port 9999 was already occupied by a detached `next-server`; after stopping that specific PID, rerunning `./harness verify` passed.
